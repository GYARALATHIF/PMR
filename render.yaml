services:
  - type: web
    name: patient-risk-predictor
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: USE_BIOCLINICAL_BERT
        value: "false"
